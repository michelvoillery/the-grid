services:

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./data/uploads:/uploads

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
